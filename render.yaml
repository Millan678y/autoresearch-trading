services:
  - type: web
    name: autoresearch-trading
    runtime: python
    buildCommand: pip install flask numpy pandas requests yfinance
    startCommand: python dashboard.py
    envVars:
      - key: PORT
        value: 5000
      - key: PYTHON_VERSION
        value: 3.11.0
    plan: free
